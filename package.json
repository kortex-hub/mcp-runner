{
  "name": "mcp-runner",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test:unit": "vitest run --passWithNoTests",
    "build:mcp-registry-client": "cd packages/mcp-registry-client &&  pnpm run build",
    "build:mcp-runner": "cd packages/mcp-runner &&  pnpm run build",
    "build:mcp-manager": "cd packages/mcp-manager &&  pnpm run build",
    "build": "pnpm run build:mcp-registry-client && pnpm run build:mcp-runner && pnpm run build:mcp-manager",
    "typecheck:mcp-manager": "tsc --noEmit --project packages/mcp-manager",
    "typecheck:mcp-runner": "tsc --noEmit --project packages/mcp-runner",
    "typecheck:mcp-registry-client": "tsc --noEmit --project packages/mcp-registry-client",
    "typecheck": "pnpm run /typecheck:.*/"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@tsconfig/node22": "^22.0.2",
    "@vitest/coverage-v8": "^3.0.9",
    "vite": "^7.1.3",
    "vitest": "^3.0.9",
    "typescript": "5.9.2",
    "@types/node": "^24"
  },
  "packageManager": "pnpm@10.7.1"
}
